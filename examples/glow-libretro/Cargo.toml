[package]
name = "glow-libretro"
version = "1.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
libretro = { package = "libretro-core", path = "../../crates/libretro-core" }
